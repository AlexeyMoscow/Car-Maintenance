# Leave NEXT_PUBLIC_API_BASE_URL empty to use the Next.js proxy (/api) and avoid CORS.
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
# Proxy target for /api -> backend URL
API_PROXY_TARGET=http://localhost:8080
